import copy

import cProfile
import pstats
import ujson, json
import profile


a = [[x for x in range(1000)] for x in range(1000)]

'''@profile
def list_test():
    b = copy.deepcopy(a)
    c = []
    for x in range(len(a)):
        temp = []
        for elem in a[x]:
            temp.append(elem)
        c.append(temp)
    d = [x[:] for x in a]
    e = ujson.loads(ujson.dumps(a))
    f = json.loads(json.dumps(a))

list_test()'''

# with cProfile.Profile() as pr:
#     list_test()
# stats = pstats.Stats(pr)
# stats.sort_stats(pstats.SortKey.TIME)
# stats.print_stats()

b = "1"
c = b
c = "2"
print(b, c)